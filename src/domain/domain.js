(function(window) {
    window.__domain = window.__domain || {};
  
    // API url
    window.__domain.apiUrl = 'localhost';
  
    // Whether or not to enable debug mode
    // Setting this to false will disable console output
    window.__domain.enableDebug = false;
  })(this);
  